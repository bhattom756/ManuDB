{
  "name": "manufacturing-management-system",
  "version": "1.0.0",
  "description": "Full-stack Manufacturing Management System",
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "setup": "cd backend && npm run setup",
    "prod-setup": "cd backend && npm run prod-setup"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
